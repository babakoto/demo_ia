# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    # Code genere : ne pas l'editer, ne pas l'analyser.
    - lib/**/*.freezed.dart
    - lib/**/*.g.dart
    - lib/**/*.config.dart

linter:
  # Les frontieres entre couches ne sont pas verifiees ici : `architecture_linter`
  # est incompatible avec la version d'analyzer exigee par freezed (voir
  # `pubspec.yaml`). Elles le sont par `test/architecture_boundaries_test.dart`.
  rules:
    - always_declare_return_types
    - avoid_dynamic_calls
    - avoid_print
    - avoid_redundant_argument_values
    - close_sinks
    - directives_ordering
    - prefer_const_constructors
    - prefer_const_constructors_in_immutables
    - prefer_final_locals
    - prefer_relative_imports
    - prefer_single_quotes
    - require_trailing_commas
    - unawaited_futures
    - use_build_context_synchronously
    - use_key_in_widget_constructors
    - use_super_parameters

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
